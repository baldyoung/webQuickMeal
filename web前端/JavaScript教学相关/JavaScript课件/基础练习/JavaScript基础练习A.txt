1.定义两个变量a和b，给变量a和b分别赋值1和2，输出a+b的值。

2.定义两个变量a和b，使用弹窗输入框分别给a和b赋值，输出a+b的值。

3.定义两个变量a和b，使用弹窗输入框分别给a和b赋值，如果用户输入的不是整数值，则在网页中输出“输入不合法”，如果输入的是整数值，则输出这两个整数值的和。

4.通过弹窗输入框输入一个字符串，输出该字符串的长度到网页中。

5.通过弹窗输入框分别输入两个字符串，输出这两个字符串中长度最长的字符串到网页中。

6.通过弹窗输入框输入一个字符串，将该字符串拆分成单个字符，并按顺序储存在数组中，最后倒叙输出该数组中的内容到网页中。

7.输出一个半径为50px的红色圆到网页中，并且设置该圆形在网页中水平居中。

8.使用循环，输出3个半径为30px的绿色圆形到网页中，并且设置这三个圆形在同一水平位置，并且水平居中。

9.使用弹窗输入框获取用户输入的一个正整数值k，然后使用循环输出k个半径为10px的绿色圆形。

10.使用弹窗输入框获取用户输入的一个正整数值k，然后使用循环输出k个半径为20px的绿色圆形，并且每个圆形按输出顺序编号序号（第一个圆的序号为1）。